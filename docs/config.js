window.DPM_CONFIG = Object.freeze({
  dataBase: 'data/',
  githubLatest: 'data/latest.json',
  dataIndex: 'data/index.json',
  githubRoot: 'data/github/',
  cloudflareApiUrl: '',
  autoRefreshMs: 120000,
  staleAfterMs: 25 * 60 * 1000
});
